import React from "react";
import ReactDOM from "react-dom/client";
import LeasingMap from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <LeasingMap />
  </React.StrictMode>
);
